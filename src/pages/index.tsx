import type { NextPage } from "next";
import { Template } from "../components/templates";

const Home: NextPage = () => {
  return (
    <Template title="test">
      <div>
        <p>Test</p>
      </div>
    </Template>
  );
};

export default Home;
